import { reviewers } from "./schema/reviewers";
import { reviews } from "./schema/reviews";
import { stages } from "./schema/stages";
import { todos } from "./schema/todos";
import { users } from "./schema/users";

export const schema = {
  reviewers: reviewers,
  todos: todos,
  users: users,
  stages: stages,
  reviews: reviews,
};
